---
id: OPS-001
title: Monitor and alert on service health
conformance: MUST
category: OPS
applies_to:
  role: any
  platform: any
source: gds-service-standard
tags: [observability, monitoring, alerting]
last_reviewed: 2026-07-04
---

# OPS-001: Monitor and alert on service health

## Standard

All production services MUST have monitoring and alerting in place. Teams MUST define Service Level Indicators (SLIs) and Service Level Objectives (SLOs). Alerts MUST route to a responsible team within business hours at minimum.

## Rationale

- You cannot fix what you cannot see
- Government services have a duty of care to users — downtime has real-world impact
- Proactive alerting reduces mean time to recovery (MTTR)
- SLOs provide an objective measure of service quality

## What good looks like

- Dashboard showing key SLIs: availability, latency (p50, p95, p99), error rate
- SLOs defined and published, for example:
  - **Availability SLO:** 99.9% of requests return a non-5xx response over a 30-day rolling window
  - **Latency SLO:** p95 response time < 500ms, p99 < 2000ms over a 30-day rolling window
  - **Error rate SLI:** `(total_requests - 5xx_responses) / total_requests`
- Alerts fire before users notice (error budget burn rate, not just threshold breaches)
- Runbooks exist for every alert — what to check, who to escalate to
- On-call or named support rota during service hours
- Structured logging with correlation IDs for request tracing
- Quarterly review of alert quality (noisy alerts get fixed or removed)

## What bad looks like

- No monitoring — team finds out about outages from Twitter/users
- Dashboard exists but no one looks at it
- Alerts that fire constantly and are ignored (alert fatigue)
- No runbooks — responders guess what to do
- "The contractor who set up monitoring left 2 years ago"

## Enforcement

| Mechanism | What is checked | When |
|-----------|----------------|------|
| **Periodic audit** | SLOs defined and published, dashboards exist, alert routing configured, runbooks present | Service assessment (alpha → beta → live) and quarterly reviews |
| **Ways of working** | On-call rota maintained, alert quality reviewed, incidents trigger blameless post-mortems | Ongoing — documented in team charter |
| **Automated (CI/CD)** | Monitoring-as-code files exist (e.g. Terraform alerting rules, Grafana dashboards in repo) | PR check — heuristic only; presence ≠ quality |

**Primary enforcement: Periodic audit.** This standard cannot be meaningfully validated by a pipeline. The compliance checker flags it for manual review at service assessment.

## Quick reference

**SLO definitions (add to your service catalogue):**

```yaml
slos:
  - name: availability
    target: 99.9
    window: 30d
    sli: "sum(rate(http_requests_total{code!~'5..'}[5m])) / sum(rate(http_requests_total[5m]))"
  - name: latency_p95
    target: 500  # milliseconds
    window: 30d
    sli: "histogram_quantile(0.95, rate(http_request_duration_seconds_bucket[5m]))"
```

**Prometheus alerting rule (error budget burn rate):**

```yaml
groups:
  - name: slo-alerts
    rules:
      - alert: ErrorBudgetBurnRateHigh
        expr: |
          (
            sum(rate(http_requests_total{code=~"5.."}[1h])) /
            sum(rate(http_requests_total[1h]))
          ) > (14.4 * (1 - 0.999))
        for: 5m
        labels:
          severity: critical
        annotations:
          summary: "Burning error budget 14.4x faster than sustainable (1h window)"
```

**Grafana dashboard (config-as-code reference):**

```json
{
  "title": "Service SLOs",
  "panels": [
    {
      "title": "Availability (30d rolling)",
      "type": "gauge",
      "targets": [{"expr": "sum(rate(http_requests_total{code!~'5..'}[30d])) / sum(rate(http_requests_total[30d])) * 100"}],
      "fieldConfig": {"defaults": {"thresholds": {"steps": [{"value": 99.0, "color": "red"}, {"value": 99.9, "color": "green"}]}}}
    }
  ]
}
```

## Source traceability

| Framework | Reference | URL | What it says |
|-----------|-----------|-----|--------------|
| GDS Service Standard | Point 14: Operate a reliable service | https://www.gov.uk/service-manual/service-standard/point-14-operate-a-reliable-service | "Monitor your service to find issues before users do" |
| DORA Metrics | Mean time to recovery | https://dora.dev/ | "MTTR correlates with organisational performance" |
| Google SRE Book | Chapters 4-5: SLOs | https://sre.google/sre-book/service-level-objectives/ | "Define SLIs and SLOs to measure service reliability objectively" |
